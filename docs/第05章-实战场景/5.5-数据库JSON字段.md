# 5.5 数据库 JSON 字段

[← 返回文档导航](../00-文档导航.md)

---

## 📖 场景说明

MySQL、PostgreSQL 等数据库支持 JSON 字段，使用 JSONMap 可以方便地读写 JSON 数据。

---

## 🚀 快速示例

```java
@Data
public class Product {
    private Long id;
    private String name;
    private String extInfo;  // JSON 字段
    
    // 扩展属性（不存数据库）
    @TableField(exist = false)
    private String brand;
    
    @TableField(exist = false)
    private String model;
}

// 保存前：Bean → JSON
Product product = new Product();
product.setName("手机");
product.setBrand("Apple");
product.setModel("iPhone 15");

JSONMap ext = new JSONMap("brand", product.getBrand(), "model", product.getModel());
product.setExtInfo(ext.toString());
productMapper.insert(product);

// 查询后：JSON → Bean
Product product = productMapper.selectById(id);
JSONMap ext = new JSONMap(product.getExtInfo());
product.setBrand(ext.getStr("brand"));
product.setModel(ext.getStr("model"));
```

---

<div align="center">

[← 上一节：配置文件管理](5.4-配置文件管理.md) | [返回文档导航](../00-文档导航.md) | [下一节：Excel导入导出 →](5.6-Excel导入导出.md)

</div>
