# 6.3 MyBatis 集成

[← 返回文档导航](../00-文档导航.md)

---

## 📖 集成说明

JSONMap 可以与 MyBatis 配合使用，处理 JSON 字段和动态查询。

---

## 🚀 快速示例

```java
// Mapper
@Mapper
public interface ProductMapper {
    @Select("SELECT * FROM product WHERE id = #{id}")
    Product selectById(Long id);
}

// Service
@Service
public class ProductService {
    
    public Product getById(Long id) {
        Product product = productMapper.selectById(id);
        
        // JSON 字段转换
        if (product.getExtInfo() != null) {
            JSONMap ext = new JSONMap(product.getExtInfo());
            product.setBrand(ext.getStr("brand"));
            product.setModel(ext.getStr("model"));
        }
        
        return product;
    }
}
```

---

<div align="center">

[← 上一节：SpringMVC集成](6.2-SpringMVC集成.md) | [返回文档导航](../00-文档导航.md) | [下一节：AI辅助开发 →](6.4-AI辅助开发.md)

</div>
