# 6.1 Spring Boot 集成

[← 返回文档导航](../00-文档导航.md)

---

## 📖 集成说明

dlz.comm 与 Spring Boot 完全兼容，无需任何配置即可使用。

---

## 🚀 快速开始

### 1. 添加依赖

```xml
<dependency>
    <groupId>top.dlzio</groupId>
    <artifactId>dlz.comm</artifactId>
    <version>6.6.3</version>
</dependency>
```

### 2. Controller 中使用

```java
@RestController
@RequestMapping("/api/user")
public class UserController {
    
    @PostMapping("/save")
    public R save(@RequestBody String formData) {
        JSONMap form = new JSONMap(formData);
        
        // 自动类型转换
        String name = form.getStr("name");
        Integer age = form.getInt("age");
        
        // 转换为 Bean
        User user = form.as(User.class);
        userService.save(user);
        
        return R.success();
    }
    
    @GetMapping("/info")
    public R info(@RequestParam Long id) {
        User user = userService.getById(id);
        
        // Bean → JSONMap
        JSONMap result = new JSONMap(user);
        result.set("extra.loginTime", System.currentTimeMillis());
        
        return R.data(result);
    }
}
```

---

## 💡 最佳实践

### 统一响应处理

```java
@RestControllerAdvice
public class ResponseAdvice implements ResponseBodyAdvice {
    
    @Override
    public Object beforeBodyWrite(Object body, ...) {
        if (body instanceof JSONMap) {
            // 自动添加时间戳
            ((JSONMap) body).set("timestamp", System.currentTimeMillis());
        }
        return body;
    }
}
```

---

<div align="center">

[返回文档导航](../00-文档导航.md) | [下一节：SpringMVC集成 →](6.2-SpringMVC集成.md)

</div>